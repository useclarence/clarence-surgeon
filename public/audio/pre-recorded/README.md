# Pre-recorded demo audio

Drop MP3 files in this folder so the onboarding Observe tab can play call demos.

Expected files:
- `marie-dupont-demo.m4a`
- `lucas-demo.m4a`

The UI will show a warning if these files are missing.
